# Available CLI Tools

- **rg** — ripgrep (`rg --line-number --color=never <pattern> [path]`). Use it via bash instead of grep for large codebases. Supports `-i` (case-insensitive), `-F` (literal), `--glob`, `--type`, `-C N` (context), `-U` (multiline). Installed at `~/.pi/agent/bin/rg`.
